# المساهمة في iBrowser

شكرًا لاهتمامك بالمساهمة في iBrowser! المشروع مفتوح المصدر (رخصة ISC)
ويرحّب بالمساهمات من الجميع.

## طرق المساهمة

- **الإبلاغ عن مشكلة (Issue):** افتح issue جديد مع خطوات واضحة لإعادة
  إنتاج المشكلة، ونظام التشغيل وإصدار iBrowser.
- **اقتراح ميزة:** افتح issue بوسم `enhancement` ووصّف الحاجة/الفائدة.
- **بناء إضافة (Plugin):** راجع [`PLUGINS.md`](PLUGINS.md) — لا تحتاج حتى
  لفتح Pull Request على هذا المستودع لبناء إضافة خاصة بك.
- **Pull Request:**
  1. Fork المستودع وأنشئ فرعًا من `master`.
  2. `npm install` ثم `npm start` للتأكد من أن التطبيق يعمل محليًا.
  3. اجعل التغييرات مركّزة وصغيرة قدر الإمكان مع رسائل commit واضحة.
  4. تأكد من أن الملفات المعدّلة تجتاز `node --check <file>` كحد أدنى
     (لا يوجد إطار اختبارات آلي حاليًا في المشروع).
  5. افتح Pull Request مع وصف للتغيير وسببه.

## إرشادات الكود

- لا تُدخل `nodeIntegration` أو تعطّل `contextIsolation` في أي نافذة جديدة.
- أي قناة IPC جديدة يجب إضافتها للقائمة البيضاء في `preload.js` (أو
  `pages/preload.js` للنوافذ الفرعية) — لا تُعرّض `ipcRenderer` مباشرة.
- التزم بأسلوب الكود الحالي (بدون أطر عمل/حزم بناء، JavaScript عادي).
- الواجهة بالعربية RTL افتراضيًا — حافظ على الاتساق مع النصوص الموجودة.

## قواعد السلوك

كن محترمًا وبنّاءً في النقاشات والمراجعات. الهدف هو تحسين المشروع للجميع.
